{
  "name": "@datafe/graph-core",
  "version": "0.1.12",
  "main": "dist/core.min.js",
  "types": "dist/src/controller/graph.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "webpack --config webpack.config.js --mode production",
    "release": "tsc && yarn build && npm version patch"
  },
  "dependencies": {
    "dagre": "0.8.5"
  },
  "devDependencies": {
    "lint-staged": "^7.2.2",
    "typescript": "^3.1.3",
    "webpack": "^4.41.4",
    "webpack-cli": "^4.1.0"
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "src/*": [
      "yarn lint",
      "git add ."
    ]
  }
}
