{
  "name": "mlp-graph",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn --cwd packages/graph-vue start",
    "build": "yarn --cwd packages/graph-vue && export NODE_ENV=production && vue-cli-service build",
    "biild:doc": "yarn --cwd packages/docs typedoc",
    "vuepress": "yarn --cwd packages/docs vuepress"
  },
  "dependencies": {},
  "devDependencies": {},
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "src/*": [
      "yarn lint",
      "git add ."
    ]
  }
}